import {riskListShuffle} from "./plinko/shuffle-constants";
function heavyTask(input: number): string {
  // Simulate heavy computation
  return `Processed input ${riskListShuffle}`;
}

export {heavyTask};
